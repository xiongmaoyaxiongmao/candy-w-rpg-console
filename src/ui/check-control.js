const esc = value => String(value ?? '').replace(/[&<>"']/gu,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function renderCheckControl({ name, label, description = '', checked = false, value = 'on', controls = '', selection = false }) {
    return `<label class="cw-clear-choice"><input class="cw-choice-input" type="checkbox" name="${esc(name)}" value="${esc(value)}" aria-label="${esc(label)}" ${controls ? `aria-controls="${esc(controls)}"` : ''} ${checked ? 'checked' : ''}><span class="cw-choice-mark" aria-hidden="true"><span class="cw-choice-on">✓</span><span class="cw-choice-off">−</span></span><span class="cw-choice-copy"><span class="cw-choice-heading"><strong>${esc(label)}</strong><span class="cw-choice-status" aria-hidden="true"><span class="cw-choice-on">${selection ? '已选中' : '已启用'}</span><span class="cw-choice-off">${selection ? '未选中' : '未启用'}</span></span></span>${description ? `<small>${esc(description)}</small>` : ''}</span></label>`;
}
