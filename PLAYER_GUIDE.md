# Candy W 上帝导演 3.1 用户说明

Candy W 为单角色聊天提供剧本、章节、角色数值和技能规则。你在聊天框描述行动，导演根据当前场景处理推进，再由酒馆生成角色演出。

## 1. 开始之前

需要安装前端扩展及配套服务端插件。只安装前端，无法使用独立导演接口。打开单角色聊天，选好角色卡、Persona 和酒馆主 API，然后点击浮动的 ✦ 打开面板。

两套接口负责不同工作：

- **导演 API**：编写、改写剧本，理解行动及规划推进。
- **酒馆主 API**：生成角色正文，沿用角色卡、Persona 和预设。

在面板的 **API 设置** 中，新建接口配置，填写名称、接口地址、密钥和模型并保存。再在导演 API 选择框选中该配置，保存使用设置。接口地址按服务商要求填写；需要 `/v1` 的服务应包含这一部分。

“获取模型列表”用于选择模型；“验证 JSON 连接”会发出一次小型真实请求，可能产生接口费用。高级生成设置通常保留默认值，遇到格式兼容问题再调整。导演不会自动改用酒馆主 API。

## 2. 最快开始一段旅程

1. 进入 **剧本库**，选择剧本，打开 **设置与绑定**。
2. 填写开场称呼、补充设定，检查数值与技能。
3. 点击 **保存剧本设置**，可保留尚未完成的填写。
4. 完成必要项目后，点击 **保存并绑定当前聊天**。
5. 点击 **进入世界**，等待开场演出。
6. 在酒馆聊天框直接输入行动并发送，例如“我走到门边，观察外面的动静”。

绑定本身不请求模型，进入世界才生成开场。普通推进直接使用聊天框；需要公开判定时，按面板提示投骰并完成本轮。

导演依据剧本已有场景、路线和条件推进，并非每次输入都会即时新增一条剧情分支。上一轮正在演出、等待投骰或恢复时，先处理完本轮。关闭面板不会结束旅程。

## 3. 创作自己的故事

点击 **创作我的故事**，填写“我想写的故事”。可以补充开场、氛围和更多设定，也可以交给导演补全。

章节数量可提前设定，也可留为 0，让导演决定。章节模块可以添加、删除、拖动排序，或使用上移、下移。点击折叠的章节标题后，填写章节名称与这一章发生的事情。

勾选“使用当前聊天的世界书”时，导演使用酒馆实际激活的条目。没有命中可补充关键词后重试，已填写的想法会保留。

生成按阶段完成。中断或失败时，可从保留的阶段继续。生成出来的数值规则若不完整，会作为草稿保留并提示修改；完成必要规则后才能绑定旅程。

## 4. 阅读、编辑和管理剧本

完整剧本是导演视图，包含秘密、动作后果和结局，阅读可能提前知道剧情。

- 顶部分段控件用于切换概览、章节、场景等类别。“全部”保留故事概览，不再重复列出全部章节。
- 点击文字卡片的编辑入口，可直接修改该项，再保存或取消。
- **直接编辑文字**用于修改现有内容；涉及新增场景、分支或整体结构时，使用导演改写。
- **让导演改写**默认折叠。展开后选择范围、填写意见，生成预览；满意后保存改写版本，也可另存副本或放弃预览。
- **另存副本**保留另一份独立剧本。编辑内置内容时会使用副本。
- **导出剧本包**用于备份和分享完整剧本。
- 删除的剧本可从已删除区域恢复；内置剧本隐藏后也可恢复。

已开始的聊天保存自己的固定剧本。修改剧本库和开局设置影响后续绑定，不会直接覆盖正在进行的旅程。

## 5. 调整章节顺序

在“章节”中展开需要修改的模块，调整标题、内容及顺序，再点击 **按此大纲重新衔接剧情**。导演生成预览后，确认保存才更新剧本。

生成前可调整大纲，生成后也可以移动章节并重新衔接。仅拖动模块还不等于已经生成并保存了新的剧情连接。

## 6. 玩到一半加入新章节

打开当前旅程的章节入口，选择 **中途加入章节**：

1. 填写想法，生成章节草稿，也可以手动填写。
2. 修改章节名称、内容、场景和衔接说明；不满意可填写意见重新生成。
3. 选择 **立即接入** 或 **当前章节结束后接入**。
4. 确认接入后，新章节才成为当前旅程的一部分。

保存草稿不会改变故事进度。立即接入时需选择结束后的返回位置；章节结束后接入会在原章节离开时进入新章节，再回到原路线。若编辑期间故事已经推进，先更新接入位置至当前进度。

新增章节仅影响当前聊天的旅程，不会自动修改剧本库原件。等待判定或恢复时，先处理当前轮次再接入。

## 7. 数值与技能

每项以列表卡片显示。点击条目或“操作”可编辑，也可侧滑操作、停用或删除。数值直接输入整数，不再使用数字滚轮。

**普通数值**有当前值、最小值和最大值，例如体力。增减结果受上下限约束。

**技能**将“是否学会”和熟练度分开。技能需要启用、已经学会、符合使用条件，并且实际尝试使用，才判断是否触发：

| 熟练度 | 触发方式 |
| --- | --- |
| 0 | 不会成功触发 |
| 1～99 | 投 d100，点数不大于熟练度时触发 |
| 100 | 满足条件时直接触发，无需投骰 |

技能不叠加旧的行动加值。提及技能或练习技能，不等于实际施放；成长规则可以另行规定练习时增加熟练度。

### 增减规则怎么填

一条有效规则需要触发条件、非零整数变化量和生效时机。例如：

| 触发条件 | 变化量 | 生效时机 |
| --- | --- | --- |
| 完成一次充分休息 | +10 | 尝试或练习时 |
| 使用技能成功 | +2 | 触发／判定成功后 |
| 使用技能失败 | +1 | 触发／判定失败后 |

“尝试或练习时”表示满足条件即可，不要求成功；其余两项要求对应判定结果。变化量填负数表示减少，0 表示没有变化，应删除这条规则。熟练度 100 的直接触发也适用成功规则。

还可设置达到某个数值时的反应。停用条目后，它不参与自动增减或技能触发。每名玩家最多 12 项，每项最多 4 条增减规则和 4 条数值反应。

生成内容不满意时可直接修改。出现规则提示，可点击对应修改入口定位问题；草稿能够保存，但未完成必要规则前不能绑定。

### 修改正在使用的角色状态

进入 **角色状态 → 编辑规则与手动修正**，修改当前数值、规则或称呼并保存。这里修改的是当前旅程；剧本库“设置与绑定”保存的是以后开局使用的设置。

名字可以随明确确认的改名剧情变化，也可以手动修正。其他角色改名、假设或尚未采用的建议，不应覆盖你的称呼。

## 8. 常见提示与恢复

| 提示或现象 | 含义与处理 |
| --- | --- |
| 增减规则需填写触发条件、非零整数变化量和生效时机 | 有规则缺项或变化量为 0。修改被标记的字段，或删除不需要的规则。 |
| 动作引用未知的 items、clues 等 | 模型生成的结构引用了不存在的项目。保留当前编写进度，从失败阶段继续修正生成。 |
| 演出消息命中本轮明确禁止短语 | 回复可能提前泄露尚未公开的信息，因此没有提交本轮。使用面板恢复入口重试。 |
| JSON 连接失败或生成内容不完整 | 检查导演地址、密钥、模型及格式兼容性；内容截断时检查输出上限，再继续失败阶段。 |
| 暂时不能继续发送 | 当前轮可能仍在生成、等待判定或恢复，先完成面板显示的待处理步骤。 |

出现恢复页面时，使用 **从这里重试**。允许放弃时可选择放弃未完成的推进；已公开投出的结果不能通过反复重试重新投骰。

活动旅程中，不要用酒馆的重新生成、滑动重生成等方式替代导演恢复操作，以免与已经确定的剧情状态冲突。

## 9. 保存、备份与隐私

剧本设置和各聊天的旅程进度分别保存。导出不是日常保存的必经步骤：剧本包用于备份剧本，旅程备份用于保留当前进度。

导出的故事可能包含你写入的人物、设定和秘密；旅程备份可能包含身份与进度信息，分享前自行确认内容。API 密钥保存在酒馆用户设置中，不属于剧本包内容。不要公开用户设置、聊天文件、旅程备份或包含密钥的截图。

结束旅程会清除当前聊天的导演进度，保留聊天正文和剧本库；如需以后恢复，先导出旅程备份。
