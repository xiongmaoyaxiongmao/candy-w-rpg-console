{
    "private": true,
    "type": "module",
    "scripts": {
        "test": "node --test tests/*.test.mjs",
        "check": "node tests/static-check.mjs",
        "verify": "npm run check && npm test"
    }
}
